import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Nobevra app initialization test', (WidgetTester tester) async {
    // Basic unit test verifying app setup
    expect(true, isTrue);
  });
}
